# Pop COSMIC Suite 0.1.0 beta 1 release checklist

Release tag: `v0.1.0-beta.1`
Debian version: `0.1.0~beta1-1`
Target: Debian 13, GNOME Shell 48, amd64
Status: **approved for publication**

The release owner records exact commands, results, dates, relevant journal
extracts, and CI links here. A checked box without evidence is not a pass.

## Automated evidence

- [x] `make test` passes from a clean checkout.
- [x] `make build-release DESTDIR=<staging-root>` builds launcher and themes.
- [x] `make test-install` installs and removes every suite-owned staged file.
- [x] `dpkg-buildpackage --no-sign --build=binary` produces one amd64 package.
- [x] Package contains no wallpapers and no `gschemas.compiled`.
- [x] Clean Debian 13 container install, doctor, and purge lifecycle passes.
- [x] Rust advisory audit reports no known vulnerability accepted by policy.
- [x] Rust license/source audit has no unknown or incompatible dependency.
- [x] Secret scan passes.
- [x] CI dependency inventory artifact is attached to the release run.

CI run URL: https://github.com/matheusdmlopes/gnome-cosmic-pop22/actions/runs/31922264227
Commit SHA: `54bd7ff`

### Local implementation evidence: 2026-08-15

- `make test` passed on the implementation tree with Rust 1.85.0: 59 ESM
  files, 8 LauncherService cases, 8 GridMetrics cases, 81 Python tests, 10 Rust unit tests plus
  doctests, rustfmt, Clippy with warnings denied, release-contract checks, and
  doctor behavior.
- `make test-install` passed with an isolated staging root and no suite-owned
  file or symlink left after uninstall. System installs omit extension-local
  schema directories; development installs keep valid compiled local schemas.
- A clean `debian:trixie-slim` container built the amd64 package with Debian's
  Rust 1.85.0. Package metadata reports version `0.1.0~beta1-1`; inspection
  found no wallpapers, background metadata, or `gschemas.compiled`.
- `cargo-deny 0.20.2 --workspace --all-features --locked` passed advisories,
  licenses, and sources. Git dependency licenses are pinned to verified files:
  `cosmic-client-toolkit` is GPL-3.0-or-later and `switcheroo-control` is
  MPL-2.0.

## X11 acceptance

Environment details and date: Debian 13 (trixie), GNOME Shell 48.7 on X11, AMD Ryzen 5 4500, NVIDIA GTX 750 Ti (driver 535.247.03), 2026-08-15.

- [x] Clean installation under `~/.local` completes without warnings.
- [x] `pop-cosmic`, `cosmic-workspaces`, and `pop-shell` enable without journal errors.
- [x] Disabling all three and enabling them again works without a Shell restart loop.
- [x] Original Super and workspace keybindings are restored after disable.
- [x] Pop Launcher opens through Super, its D-Bus method, and its shortcut.
- [x] Pop Launcher dark theme contrast and label visibility verified.
- [x] Applications modal grid scrolling verified (mouse wheel and touchpad continuous scrolling).
- [x] Applications modal dismiss on first outside click verified.
- [x] Applications modal auto-dismiss upon launching applications verified.
- [x] Pop Settings opens from the app menu and each extension preferences entry.
- [x] Settings update live and persist after logout/login.
- [x] Dash-to-Dock and DING coexist without blocking core suite behavior.

Evidence/journal notes: Verified by Matheus on real hardware. All test seams and interaction mechanics operational.

## Wayland acceptance

Environment details and date: Deferred to Beta 2 (X11 is the primary validated target for Beta 1 on hardware with proprietary NVIDIA drivers).

## Migration acceptance

- [x] `pop-cosmic-doctor` detects each existing `~/.local` extension copy.
- [x] Migration guidance backs up local copies and never deletes them automatically.
- [x] Clean development installation under `~/.local` verified and operational.

## Known limitations

Minor cosmetic issues allowed for this beta:
- Wayland session verification deferred to Beta 2.
- Debian `.deb` package built and validated in CI container; local environment uses `~/.local` source installation.

## Human approval

X11 approver/date: Matheus De Marco Lopes / 2026-08-15
Release owner/date: Matheus De Marco Lopes / 2026-08-15

Approved for public beta tag `v0.1.0-beta.1` and GitHub Prerelease publication.
